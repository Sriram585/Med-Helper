name: med_helper_app
description: A new Flutter project for Med Helper.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.1.0
  shared_preferences: ^2.2.0
  provider: ^6.0.5
  google_fonts: ^6.1.0
  file_picker: ^6.0.0
  intl: ^0.18.1
  fl_chart: ^0.65.0
  animate_do: ^3.1.0  # For animations
  glassmorphism: ^3.0.0 # For glass effect easily or just custom implementation

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
